import { useEffect, useState } from 'react'
import { Building2, Plus, X } from 'lucide-react'
import { createDepartment, getDepartments } from '../api'

export default function Departments(){const [deps,setDeps]=useState([]),[open,setOpen]=useState(false),[form,setForm]=useState({name:'',code:'',description:''}),[error,setError]=useState('');const load=()=>getDepartments().then(r=>setDeps(r.data));useEffect(()=>{load()},[]);const save=async e=>{e.preventDefault();setError('');try{await createDepartment(form);setForm({name:'',code:'',description:''});setOpen(false);load()}catch(err){setError(Object.values(err.response?.data||{}).flat().join(' ')||'Could not create department.')}};return <div className="space-y-5"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><p className="text-sm text-slate-500">{deps.length} academic departments</p><button className="btn-primary" onClick={()=>setOpen(true)}><Plus size={18}/> Add department</button></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{deps.map(d=><div className="card p-5" key={d.id}><div className="flex items-start justify-between"><div className="grid h-11 w-11 place-items-center rounded-xl bg-indigo-50 text-indigo-600"><Building2 size={21}/></div><span className="rounded-lg bg-slate-100 px-2.5 py-1 text-xs font-bold text-slate-600">{d.code}</span></div><h3 className="mt-5 font-bold">{d.name}</h3><p className="mt-1 min-h-10 text-sm text-slate-500">{d.description || 'Academic department'}</p><div className="mt-5 border-t border-slate-100 pt-4 text-xs font-semibold text-slate-500">{d.student_count} student{d.student_count===1?'':'s'}</div></div>)}{!deps.length&&<div className="card col-span-full p-12 text-center text-sm text-slate-500">No departments yet.</div>}</div>{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4"><form onSubmit={save} className="w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><div className="flex items-center justify-between"><div><h3 className="text-lg font-extrabold">Add department</h3><p className="text-xs text-slate-500">Create a new academic department.</p></div><button type="button" onClick={()=>setOpen(false)} className="rounded-lg p-2 hover:bg-slate-100"><X size={18}/></button></div><div className="mt-6 space-y-4"><label><span className="label">Department name</span><input required className="input" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label><span className="label">Code</span><input required className="input" value={form.code} onChange={e=>setForm({...form,code:e.target.value.toUpperCase()})}/></label><label><span className="label">Description</span><textarea className="input min-h-24 resize-none" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label>{error&&<p className="text-sm text-rose-600">{error}</p>}</div><div className="mt-6 flex justify-end gap-2"><button type="button" onClick={()=>setOpen(false)} className="btn-secondary">Cancel</button><button className="btn-primary"><Plus size={16}/> Create</button></div></form></div>}</div>}
